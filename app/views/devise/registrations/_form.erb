<%# Form SASS %>
<%= stylesheet_link_tag '/objects/_forms.scss'%>


<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <div class='form-container'>
    <div class='row-register'>
      <div class='col-1-register'>
        <div class='form'>
          <%= render "devise/shared/error_messages", resource: resource %>
          <div class="field">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", :class => 'text-field', :placeholder => "Email" %>
          </div>
          <div class="field">
            <%= f.password_field :password, autocomplete: "new-password", :class => 'text-field', :placeholder => "Password" %>
          </div>
          <div class="field">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", :class => 'text-field', :placeholder => "Password Confirmation" %>
          </div>
          <div class="actions">
            <%= f.submit "Create Account", :class => 'btn' %>
          </div>
        </div>
      </div>
      </div>
    </div>
<% end %>
