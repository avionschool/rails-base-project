<h4>Hi user <%= @resource.username %>!</h4>

<p>You can confirm your account email <%= @email %> through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
