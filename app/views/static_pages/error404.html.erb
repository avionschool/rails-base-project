<% unless request.fullpath.start_with?('/admins/') || request.fullpath.start_with?('/users/') %>
  <div class="error404">
    
    <main>
      <%= render 'e' %>
    </main>
    
  </div>

  <% else %>

  <div>

    <main>
      <%= render 'e' %>
    </main>
    
  </div>

<% end %>
