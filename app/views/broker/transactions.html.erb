<h1>Broker Transactions Page</h1>

<% if @transactions.empty? %>
    <p>None of your stocks have been purchased yet.</p>
<% else %>
    <table>
        <thead>
            <tr>
            <th>Date Purchased</th>
            <th>Ticker</th>
            <th>Company</th>
            <th>Price</th>
            <th>Buyer</th>
            <th>Quantity</th>
            <th>Total Cost</th>
            </tr>
        </thead>

        <tbody>
            <% @transactions.each do |stock| %>
            <tr>
                <td><%= stock.created_at.to_formatted_s(:long) %></td>
                <td><%= stock.ticker %></td>
                <td><%= stock.company_name %></td>
                <td>$<%= stock.stock_price %></td>
                <td><%= stock.buyer.email %></td>
                <td><%= stock.quantity %></td>
                <td>$<%= ((stock.quantity).to_i * (stock.stock_price).to_f).to_f %></td>
            </tr>
            <% end %>
        </tbody>
    </table>
<% end %>
<%= link_to "Back", broker_index_path %>